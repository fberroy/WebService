{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    	<br></br> 
	<div class="container" ,style=" text-align: center;">
	<a href="#" onclick="javascript:history.go(-1);"class="btn btn-secondary btn-sm">Retour</a>
        <h3><br> Les manques :  </h3>

        <table class="table">
            <thead>
                <tr>
                    <th>Nom</th>
                    <th> Enseignant(s) requis </th>
		    <th> Enseignant(s) inscrit </th>
                </tr>
            </thead>
            <tbody>
            {% for cours in manques5 %}
                <tr>
                    <td><h5>{{ cours[0].nomCours }}</h5></td>
		    <td><h5>{{ cours[0].NbEnseignants }}</h5></td>
		    <td><h5>{{ cours[1]}}</h5></td>

                    <td style="text-align: right;">
                        <a href="{{ path('app_cours_show', {'id': cours[0].id}) }}"> <button type="button" class="btn btn-primary btn-sm">Voir</button></a>
                    </td>
                    <td style="text-align: right;">
                        <a href="{{ path('app_cours_edit', {'id': cours[0].id}) }}"> <button type="button" class="btn btn-secondary btn-sm">Modifier</button></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">Pas de manques trouvés dans la base de données</td>
                </tr>
            {% endfor %}


	
            </tbody>
        </table>
        </div>
</div>
       


	
            </tbody>
        </table>
        </div>
        <div class="col">
    
</div>

{% endblock %}


